
const React = require('react-native');

const { StyleSheet } = React;

module.exports = StyleSheet.create({
  sidebar: {
    flex: 1,
    paddingTop: 30,
    paddingBottom: 30,
    backgroundColor: '#fff',
  },
});
